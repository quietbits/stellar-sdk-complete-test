# Plug'n'Play: no node_modules at all — resolution goes through .pnp.cjs.
# This is the layout most likely to expose a package that relies on
# non-standard resolution, so it's the highest-signal package-manager test.
nodeLinker: pnp
enableTelemetry: false
