# `@stellar/stellar-sdk@16.2.0` — re-test report

| | |
|---|---|
| SDK version | `16.2.0` |
| Previous baseline | `16.2.0` |
| Date | `2026-07-31` |
| Verdict | `BLOCKED WITH KNOWN FINDINGS` — ISSUES.md issues 12, 14, and 15 |

## Toolchain

| Tool | Version |
|------|---------|
| Node | 22.22.0 |
| Deno | 2.8.3 |
| Bun | 1.3.14 |
| npm | 10.9.4 |
| pnpm | 10.33.2 |
| Yarn classic | 1.22.22 |
| Yarn Berry | 4.5.0 |
| corepack | 0.34.0 |
| Chrome / Chromium | Google Chrome 151.0.7922.71 |

## Resolved dependencies

Recorded from a clean `pnpm install` because the SDK's own dependencies use ranges and the same SDK version can resolve a different tree over time.

| Package | Resolved |
|---------|----------|
| `@stellar/js-xdr` | 4.0.0 |
| `axios` | 1.18.0 |
| `@noble/*` | `ed25519` 3.1.0, `hashes` 2.2.0 |

## Changelog review

Versions crossed: `16.2.0` → `16.2.0`

- **Breaking changes:** none to review; this was a same-version re-test.
- **New / removed public API:** none; the live inventory remains 510 symbols.
- **Fixes touching covered surface:** none to review; no version boundary was crossed.

## Results

### Runtime axis

| Runtime | Deterministic (`STELLAR_LIVE=0`) | Live | Notes |
|---------|----------------------------------|------|-------|
| Node | 446 pass / 4 fail (450 tests) | 452 pass / 4 fail (456 tests) | All 4 failures match baseline issue 4 |
| Deno | 54 passed, 490 steps / 2 failed (4 steps) | 54 passed, 496 steps / 2 failed (4 steps) | Same 4 expected failing steps; `--no-check` required |
| Bun | 443 pass / 4 fail (447 tests) | 449 pass / 4 fail (453 tests) | Same 4 expected failures |

The deterministic and live aggregate commands reached all three runtimes despite Node's expected non-zero exit. The live run added six net tests because seven live tests replaced one offline placeholder.

### Package-manager axis

| Manager | Modules | CLI bin |
|---------|---------|---------|
| npm | PASS | PASS |
| pnpm | PASS | PASS |
| yarn-classic | PASS | PASS |
| yarn-berry (PnP) | FAIL — issue 12 | FAIL — issue 15 |

Yarn Berry's module smoke reproduces `ERR_REQUIRE_ESM` when the SDK's CJS build requires `@noble/hashes/sha2.js`. Its independently-run `stellar-js --help` smoke reproduces `EBADF: bad file descriptor, fstat` in the PnP ESM loader. Both failures match the current baseline.

### Browser axis

| Artifact | Result |
|----------|--------|
| `stellar-sdk.js` | PASS |
| `stellar-sdk.min.js` | PASS |
| `stellar-sdk-axios.js` | PASS |
| `stellar-sdk-axios.min.js` | PASS |

All four artifacts loaded through real script elements in Chrome and passed global attachment, signing, XDR conversion, and same-origin Horizon HTTP checks.

### CLI and artifact axis

| Check | Result |
|-------|--------|
| `stellar-js --help` / `--version` | PASS under Node, npm, pnpm, and Yarn classic; Yarn Berry FAIL (issue 15) |
| Invalid arguments | PASS |
| Generate from local WASM | PASS |
| Generated package type check and load | FAIL as emitted (issue 14); PASS after the test supplies Node types |
| Exports, bin and `dist/` artifact inventory | PASS |

The CLI generation test reproduces the known `TS2591` failure caused by generated `Buffer` types without Node globals, then applies the documented consumer workaround and proves the generated package compiles and loads.

### Type check

| | |
|---|---|
| `tsc --noEmit` errors | 27 |
| Change vs baseline | No change |

## Baseline diff

Diffed against [`baseline.json`](baseline.json), including its runtime, package-manager, browser, CLI, type, and coverage axes.

### Expected failures (matched baseline)

1. `stellar-sdk public API surface` › `matches expected root exports`
2. `stellar-sdk public API surface` › `matches expected contract exports`
3. `stellar-sdk public API surface` › `matches expected nested rpc and contract namespace exports`
4. `stellar-sdk class and method surface locks` › `keeps Keypair static and instance methods stable`
5. Yarn Berry CJS module resolution fails with `ERR_REQUIRE_ESM` — issue 12.
6. Generated bindings fail their emitted TypeScript build with `TS2591` — issue 14.
7. Yarn Berry's installed CLI fails with `EBADF` — issue 15.

### New failures

None.

### Newly passing

None.

## Coverage of new code

From `coverage-audit.mjs`, which diffs the live public surface against `surface-inventory.json` and guards exports-map drift.

| | |
|---|---|
| Public symbols | 510 |
| New since last inventory | 0 |
| New symbols **without** a behavior test | 0 |
| Pre-existing backlog (not blocking) | 120 reported / 122 corrected for known false positives |

The upstream cross-reference reports all 120 local-backlog names somewhere in upstream tests and zero names with no upstream match. This remains heuristic word-boundary matching, not proof of meaningful behavioral coverage; the two known false positives remain recorded in the baseline. Forty-one symbols are deliberately excluded with reasons.

### New symbols and their coverage

None; this was a same-version re-test and the inventory did not change.

### Behavior changes to existing symbols

None identified; no changelog versions were crossed.

### Tests added this run

None during the release run. The run validated the previously added CLI, package-artifact, browser, and aggregate-runner coverage without modifying tests.

## Surface changes

Compared by set difference against the 16.2.0 inventory.

| Lock | Before → after | Added | Removed |
|------|----------------|-------|---------|
| root exports | 80 → 80 actual (`77` locked) | none | none |
| `contract` exports | 11 → 11 actual (`10` locked) | none | none |
| `Keypair` instance | 16 → 16 actual (`13` locked) | none | none |

There are no new additions or removals. The four surface-lock failures remain the deliberately deferred additive drift recorded as issue 4.

## Skipped

Nothing skipped. Node, Deno, Bun, all four package managers, all four browser artifacts, the installed CLI checks, type checking, the surface audit, and the upstream cross-reference ran.

## Follow-ups

- No baseline update is required because every measured count and outcome matches `reports/baseline.json`.
- Issues 12, 14, and 15 remain blocking SDK findings; no new finding was discovered.
- Preserve this report alongside [`16.2.0.md`](16.2.0.md); the earlier report records the original release test and subsequent coverage history, while this file records the complete expanded consumer-axis re-test.
